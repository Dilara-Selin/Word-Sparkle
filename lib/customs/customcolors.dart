import 'package:flutter/material.dart';

class CustomColors {
  static const scaffoldBackGroundColor = Color(0xFFF48F81);
  static const textButtonColor = Color.fromARGB(255, 0, 0, 0);
  static const loginButtonTextColor = Color(0xffffffff);
  static const pinkcolor = Color(0xFFFBE1EF);
  static final buttoncolor = Colors.black.withOpacity(0.75);
  static const blackcolor = Colors.black;
  static const whitecolor = Colors.white;
  static const errorcolor = Color(0xFF756AB6);
  static const hintText = Colors.grey;
}
